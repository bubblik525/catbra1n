import data from './cortex-data.mjs';
import {propagate} from './cortex-engine.mjs';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function mountCortex(host){
 let timer=null,step=0,run=null,chosen=data.examples[0];
 const points=Array.from({length:65},(_,i)=>{const angle=i*2.399963,rad=Math.sqrt((i+.5)/65);return [300+Math.cos(angle)*rad*206,220+Math.sin(angle)*rad*143];});
 host.innerHTML=`<section class="cx-lab"><div class="cx-top"><span><i></i> CAT BRAIN / CORTEX 001</span><span>LOCAL COMPUTE · EXPERIMENTAL</span></div><div class="cx-grid"><div class="cx-scene"><div class="cx-title"><span>THE SHAPE OF INSTINCT</span><h2>A small brain.<br>A different perspective.</h2></div><svg class="cx-network" viewBox="0 0 600 410" role="img" aria-label="65 cortical regions. Animated activations computed by a small experimental network."><defs><radialGradient id="cx-glow"><stop stop-color="#bcd6eb" stop-opacity=".10"/><stop offset="1" stop-color="#000" stop-opacity="0"/></radialGradient></defs><ellipse cx="300" cy="225" rx="270" ry="180" fill="url(#cx-glow)"/><path class="cx-outline" d="M106 250 Q69 183 119 75 L178 121 Q300 74 425 121 L481 75 Q533 178 493 251 Q470 357 302 371 Q144 366 106 250Z"/><g class="cx-links">${data.edges.map(([a,b])=>`<path d="M${points[a].join(' ')} L${points[b].join(' ')}"/>`).join('')}</g><g>${points.map(([x,y],i)=>`<circle class="cx-node" cx="${x}" cy="${y}" r="2.5"><title>Region ${i+1}</title></circle>`).join('')}</g><path class="cx-eyes" d="M204 234 Q226 213 246 234 M354 234 Q374 213 396 234"/><path class="cx-whiskers" d="M110 257 L55 246 M114 268 L48 277 M490 257 L545 246 M486 268 L552 277"/></svg><div class="cx-phase">AWAITING A MEMORY</div><div class="cx-stats"><div><b>65</b><span>CORTICAL REGIONS</span></div><div><b>1,139</b><span>DIRECTED CONNECTIONS</span></div><div><b>8</b><span>COMPUTE STEPS</span></div></div></div><div class="cx-console"><span class="cx-eyebrow">01 / HISTORICAL REPLAY</span><h3>Let the cat trace a pattern.</h3><p>Replay a held-out Pons launch through the network. Contract research below uses your connected provider.</p><label>CHOOSE AN UNSEEN TRAINING-TEST CASE<select class="cx-select">${data.examples.map((e,i)=>`<option value="${i}">${esc(e.symbol||'Unnamed')} · ${esc(e.time.slice(11,16))} UTC</option>`).join('')}</select></label><div class="cx-address"></div><div class="cx-inputs"></div><button class="cx-run">Run cortical replay ↗</button><div class="cx-result" aria-live="polite"><small>READOUT / NOT CALIBRATED</small><strong>Ready when you are.</strong><p>Five measured features. A published connection graph. A small trained output layer.</p></div><div class="cx-trace">INPUT → PROPAGATION → READOUT → OBSERVED OUTCOME</div></div></div><details class="cx-method"><summary>Inside the experiment <span>227 training cases · 98 held-out cases ↗</span></summary><p>Published cat cortical connectivity from NeuroData; each node represents an area, not a neuron. Layout, input weights and signal dynamics are engineered. Only the logistic readout is trained. One first-record example per token, chronological 70/30 split.</p><p>Target: next available record closes lower. This is a variable horizon, not a next-day forecast. 675 of 1,000 sampled tokens lack enough records. Brier score: ${data.training.brier.toFixed(4)}; constant baseline: ${data.training.baselineBrier.toFixed(4)} (lower is better). This small one-day test does not establish a trading edge.</p><a href="${data.source}" target="_blank" rel="noreferrer">Connectivity source ↗</a></details></section>`;
 const names=['Candle return','Price range','Volume intensity','Close position','Distance from high'];
 function select(){clearInterval(timer);timer=null;step=0;chosen=data.examples[Number(host.querySelector('select').value)];run=propagate(chosen.features);host.querySelector('.cx-address').textContent=chosen.token;host.querySelector('.cx-inputs').innerHTML=chosen.features.map((v,i)=>`<div><span>${names[i]}</span><b>${v.toFixed(2)}</b><i style="--fill:${(v+1)*50}%"></i></div>`).join('');host.querySelector('.cx-run').textContent='Run cortical replay ↗';host.querySelector('.cx-result strong').textContent='Ready when you are.';host.querySelector('.cx-result p').textContent='Replay uses only the first available record as input.';paint();}
 function paint(){host.querySelectorAll('.cx-node').forEach((n,i)=>{const v=Math.abs(run.frames[step][i]);n.setAttribute('r',String(2+v*4));n.style.opacity=String(.25+v*.75);});host.querySelector('.cx-phase').textContent=step===0?'INPUT READY':step===8?'READOUT COMPLETE':`PROPAGATING / ${String(step).padStart(2,'0')} OF 08`;host.querySelector('.cx-lab').classList.toggle('cx-running',step>0&&step<8);}
 function done(){clearInterval(timer);timer=null;host.querySelector('.cx-run').textContent='Replay again ↗';host.querySelector('.cx-result strong').textContent=run.score>=.5?'Decline-leaning pattern':'No decline preference';host.querySelector('.cx-result p').textContent=`Raw model score ${(run.score*100).toFixed(1)}/100, not a calibrated probability. Observed next-record change: ${chosen.change>=0?'+':''}${chosen.change.toFixed(2)}%. ${chosen.time.slice(11,16)} → ${chosen.nextTime.slice(11,16)} UTC (${chosen.nextTime.slice(0,10)}).`;}
 host.querySelector('select').onchange=select;
 host.querySelector('.cx-run').onclick=()=>{if(timer){clearInterval(timer);timer=null;host.querySelector('.cx-run').textContent='Resume replay ↗';return;}if(step===8)step=0;if(matchMedia('(prefers-reduced-motion: reduce)').matches){step=8;paint();done();return;}host.querySelector('.cx-run').textContent='Pause replay';timer=setInterval(()=>{step++;paint();if(step===8)done();},330);};select();
 return ()=>clearInterval(timer);
}
