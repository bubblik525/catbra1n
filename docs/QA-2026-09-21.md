# Cat Brain QA — 2026-09-21

## Verified

- Native Electron window smoke test passed (`DESKTOP_SMOKE_OK`).

- 63 Node tests passed on macOS, including independent model reference outputs, formula behavior, HTTP authentication, persistence, provider validation, source failures, and desktop mode switching.
- PTY integration: keyboard flow, save/restore, clean exit, lock release, watch rules and simulated positions passed.
- Model training reproduced byte-identical bundled inference data. Published graph: 65 regions, 1,139 edges; 227 train / 98 holdout. Brier 0.1092032397 vs constant baseline 0.1119413241 on this limited sample.
- Desk 1: historical AGAINPAD analysis completed with 19 observations and 88.4/100 raw score; cortex mode switching and repeat analysis exercised.
- Desk 2: observed return -89.99%, close drawdown -90.02%; 24-hour horizon selection and stress slider to -10% exercised. Insufficient exact-hour history remains explicitly unavailable.
- Desk 3: authenticated live Blockscout token and address batch completed 2/2 without failure. Combined graph contained 65 objects. Holder filtering displayed 30 links; graph selection and orbit exercised. Source provenance retained. Runtime address lists are excluded from this repository.
- Desk 4: authenticated live connection confirmed; factory launches increased from 23 to 42 during the check. Display updated every second. The market source returned no quotes for this sample, so price fields stayed unknown and signals stayed WAIT.
- No credential is included in source. Runtime credentials were used only for the authenticated local test and Blockscout requests.

## Fixes made during QA

- Clamp binary-animation elapsed time to zero: the first animation frame can have a timestamp slightly before the input-event start time. Negative elapsed time previously selected an undefined snippet and stopped the animation.
- Remove both demo and recording flags when switching to live mode, so recording mode cannot silently force a demo restart.
- Authentication errors and complete address-provider outages now fail instead of producing a misleading checked empty graph. Partial source failures are labeled partial.
- Training script now reads the already-bundled public fixture, avoiding dependence on private local export folders.

## Limits

- Live Bitquery price-history requests were not authenticated: a separate Bitquery access token is needed. Archive inference and formulas were verified.
- Live market quote availability was not established for the discovered launches; no quote values or trade signals were fabricated.
- Model correctness checks establish reproducible arithmetic, not forecast accuracy or profitability.
- Address research is a bounded first-page sample, not full chain-history reconstruction. Common ownership is not inferred from transfers.
- Windows/Linux installers and production cloud deployment are not covered by this local QA pass.
- Public releases, deployments and repository visibility changes are outside this upload.
